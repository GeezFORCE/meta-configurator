name: Build and Deploy
on:
  push:
    branches:
      - shacl-bridge-integration-feat
permissions:
  contents: write
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install and Build
        env:
          USE_META_CONFIGURATOR_BASE_PATH: true  # Set to true for GitHub Pages deployment
          VITE_FRONTEND_HOSTNAME: https://geezforce.github.io/meta-configurator
          EXPERIMENTAL: true
        run: |
          cd meta_configurator
          npm ci
          npm run build
          cd dist
          cp index.html 404.html

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: meta_configurator/dist
          branch: gh-pages
          target-folder: docs
